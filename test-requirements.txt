# Testing dependencies for UXsim
# These versions are specifically chosen to work together without serialization issues

# Core pytest functionality
pytest>=7.4.0,<8.0.0

# Parallel testing support - stable version without serialization issues
pytest-xdist>=3.3.0,<4.0.0

# Flaky test retry functionality - compatible with pytest-xdist
pytest-rerunfailures>=11.1,<12.0

# Core dependencies for xdist
execnet>=2.0.0,<3.0.0

# Core pytest dependencies
pluggy>=1.0.0,<2.0.0
iniconfig>=1.0.0,<3.0.0